import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["github.com", 'randomuser.me', 'images.unsplash.com','https://plus.unsplash.com'],
  },
};

export default nextConfig;
